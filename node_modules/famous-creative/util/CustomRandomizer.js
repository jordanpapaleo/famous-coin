export class CustomRandomizer {
  constructor (fn) {
    this.fn = fn;
  }

  random() {
    var key = Math.random();
    var value = Math.random();
    var limit = this.fn(key);
    if(value < limit) return key;
    else return this.random();
  }
}